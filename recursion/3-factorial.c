#include "main.h"
/**
 * factorial - calculates factorial
 * @n: number
 *
 * Return: int
 */
int factorial(int n)
{
	if (i < 0)
		return (-1);
	else if (i <= 1)
		return (1);
	return (i * factorial(i - 1));
}
