#include "main.h"
#include "_putchar.c"
/**
 * _print_rev_recursion - puts
 * @s: string
 *
 * Return: no return
 */
void _print_rev_recursion(char *s)
{
	if (*s)
		s++;
		_print_rev_recursion(s);
	_putchar(*s);
}
int main(void)
{
    _print_rev_recursion("\nColton Walker");
    return (0);
}
